% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rotation.R
\name{astro_inverse_rotation}
\alias{astro_inverse_rotation}
\title{Calculate the inverse of a rotation matrix}
\usage{
astro_inverse_rotation(rotation)
}
\arguments{
\item{rotation}{A rotation matrix (list with \code{rot} component) as returned
by \code{\link[=astro_identity_matrix]{astro_identity_matrix()}} or other rotation functions}
}
\value{
A rotation matrix that performs the opposite transformation
}
\description{
Given a rotation matrix that performs some coordinate transform,
this function returns the matrix that reverses that transform.
}
\examples{
# Create a rotation and invert it
rot <- astro_identity_matrix()
inv <- astro_inverse_rotation(rot)
}
